// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/ynu-e-young/htp-platform/app/machine/service/internal/data/ent/capturelog"
	"github.com/ynu-e-young/htp-platform/app/machine/service/internal/data/ent/predicate"
)

// CaptureLogQuery is the builder for querying CaptureLog entities.
type CaptureLogQuery struct {
	config
	ctx        *QueryContext
	order      []OrderFunc
	inters     []Interceptor
	predicates []predicate.CaptureLog
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CaptureLogQuery builder.
func (clq *CaptureLogQuery) Where(ps ...predicate.CaptureLog) *CaptureLogQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit the number of records to be returned by this query.
func (clq *CaptureLogQuery) Limit(limit int) *CaptureLogQuery {
	clq.ctx.Limit = &limit
	return clq
}

// Offset to start from.
func (clq *CaptureLogQuery) Offset(offset int) *CaptureLogQuery {
	clq.ctx.Offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CaptureLogQuery) Unique(unique bool) *CaptureLogQuery {
	clq.ctx.Unique = &unique
	return clq
}

// Order specifies how the records should be ordered.
func (clq *CaptureLogQuery) Order(o ...OrderFunc) *CaptureLogQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// First returns the first CaptureLog entity from the query.
// Returns a *NotFoundError when no CaptureLog was found.
func (clq *CaptureLogQuery) First(ctx context.Context) (*CaptureLog, error) {
	nodes, err := clq.Limit(1).All(setContextOp(ctx, clq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{capturelog.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CaptureLogQuery) FirstX(ctx context.Context) *CaptureLog {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CaptureLog ID from the query.
// Returns a *NotFoundError when no CaptureLog ID was found.
func (clq *CaptureLogQuery) FirstID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = clq.Limit(1).IDs(setContextOp(ctx, clq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{capturelog.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CaptureLogQuery) FirstIDX(ctx context.Context) int64 {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CaptureLog entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CaptureLog entity is found.
// Returns a *NotFoundError when no CaptureLog entities are found.
func (clq *CaptureLogQuery) Only(ctx context.Context) (*CaptureLog, error) {
	nodes, err := clq.Limit(2).All(setContextOp(ctx, clq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{capturelog.Label}
	default:
		return nil, &NotSingularError{capturelog.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CaptureLogQuery) OnlyX(ctx context.Context) *CaptureLog {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CaptureLog ID in the query.
// Returns a *NotSingularError when more than one CaptureLog ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CaptureLogQuery) OnlyID(ctx context.Context) (id int64, err error) {
	var ids []int64
	if ids, err = clq.Limit(2).IDs(setContextOp(ctx, clq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{capturelog.Label}
	default:
		err = &NotSingularError{capturelog.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CaptureLogQuery) OnlyIDX(ctx context.Context) int64 {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CaptureLogs.
func (clq *CaptureLogQuery) All(ctx context.Context) ([]*CaptureLog, error) {
	ctx = setContextOp(ctx, clq.ctx, "All")
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CaptureLog, *CaptureLogQuery]()
	return withInterceptors[[]*CaptureLog](ctx, clq, qr, clq.inters)
}

// AllX is like All, but panics if an error occurs.
func (clq *CaptureLogQuery) AllX(ctx context.Context) []*CaptureLog {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CaptureLog IDs.
func (clq *CaptureLogQuery) IDs(ctx context.Context) (ids []int64, err error) {
	if clq.ctx.Unique == nil && clq.path != nil {
		clq.Unique(true)
	}
	ctx = setContextOp(ctx, clq.ctx, "IDs")
	if err = clq.Select(capturelog.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CaptureLogQuery) IDsX(ctx context.Context) []int64 {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CaptureLogQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, clq.ctx, "Count")
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, clq, querierCount[*CaptureLogQuery](), clq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CaptureLogQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CaptureLogQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, clq.ctx, "Exist")
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CaptureLogQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CaptureLogQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CaptureLogQuery) Clone() *CaptureLogQuery {
	if clq == nil {
		return nil
	}
	return &CaptureLogQuery{
		config:     clq.config,
		ctx:        clq.ctx.Clone(),
		order:      append([]OrderFunc{}, clq.order...),
		inters:     append([]Interceptor{}, clq.inters...),
		predicates: append([]predicate.CaptureLog{}, clq.predicates...),
		// clone intermediate query.
		sql:  clq.sql.Clone(),
		path: clq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		MachineID uuid.UUID `json:"machine_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CaptureLog.Query().
//		GroupBy(capturelog.FieldMachineID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CaptureLogQuery) GroupBy(field string, fields ...string) *CaptureLogGroupBy {
	clq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CaptureLogGroupBy{build: clq}
	grbuild.flds = &clq.ctx.Fields
	grbuild.label = capturelog.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		MachineID uuid.UUID `json:"machine_id,omitempty"`
//	}
//
//	client.CaptureLog.Query().
//		Select(capturelog.FieldMachineID).
//		Scan(ctx, &v)
func (clq *CaptureLogQuery) Select(fields ...string) *CaptureLogSelect {
	clq.ctx.Fields = append(clq.ctx.Fields, fields...)
	sbuild := &CaptureLogSelect{CaptureLogQuery: clq}
	sbuild.label = capturelog.Label
	sbuild.flds, sbuild.scan = &clq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CaptureLogSelect configured with the given aggregations.
func (clq *CaptureLogQuery) Aggregate(fns ...AggregateFunc) *CaptureLogSelect {
	return clq.Select().Aggregate(fns...)
}

func (clq *CaptureLogQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range clq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, clq); err != nil {
				return err
			}
		}
	}
	for _, f := range clq.ctx.Fields {
		if !capturelog.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CaptureLogQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CaptureLog, error) {
	var (
		nodes = []*CaptureLog{}
		_spec = clq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CaptureLog).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CaptureLog{config: clq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (clq *CaptureLogQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	_spec.Node.Columns = clq.ctx.Fields
	if len(clq.ctx.Fields) > 0 {
		_spec.Unique = clq.ctx.Unique != nil && *clq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CaptureLogQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(capturelog.Table, capturelog.Columns, sqlgraph.NewFieldSpec(capturelog.FieldID, field.TypeInt64))
	_spec.From = clq.sql
	if unique := clq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if clq.path != nil {
		_spec.Unique = true
	}
	if fields := clq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, capturelog.FieldID)
		for i := range fields {
			if fields[i] != capturelog.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CaptureLogQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(capturelog.Table)
	columns := clq.ctx.Fields
	if len(columns) == 0 {
		columns = capturelog.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.ctx.Unique != nil && *clq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range clq.modifiers {
		m(selector)
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// Modify adds a query modifier for attaching custom logic to queries.
func (clq *CaptureLogQuery) Modify(modifiers ...func(s *sql.Selector)) *CaptureLogSelect {
	clq.modifiers = append(clq.modifiers, modifiers...)
	return clq.Select()
}

// CaptureLogGroupBy is the group-by builder for CaptureLog entities.
type CaptureLogGroupBy struct {
	selector
	build *CaptureLogQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CaptureLogGroupBy) Aggregate(fns ...AggregateFunc) *CaptureLogGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the selector query and scans the result into the given value.
func (clgb *CaptureLogGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, clgb.build.ctx, "GroupBy")
	if err := clgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CaptureLogQuery, *CaptureLogGroupBy](ctx, clgb.build, clgb, clgb.build.inters, v)
}

func (clgb *CaptureLogGroupBy) sqlScan(ctx context.Context, root *CaptureLogQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*clgb.flds)+len(clgb.fns))
		for _, f := range *clgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*clgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CaptureLogSelect is the builder for selecting fields of CaptureLog entities.
type CaptureLogSelect struct {
	*CaptureLogQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cls *CaptureLogSelect) Aggregate(fns ...AggregateFunc) *CaptureLogSelect {
	cls.fns = append(cls.fns, fns...)
	return cls
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CaptureLogSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cls.ctx, "Select")
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CaptureLogQuery, *CaptureLogSelect](ctx, cls.CaptureLogQuery, cls, cls.inters, v)
}

func (cls *CaptureLogSelect) sqlScan(ctx context.Context, root *CaptureLogQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cls.fns))
	for _, fn := range cls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// Modify adds a query modifier for attaching custom logic to queries.
func (cls *CaptureLogSelect) Modify(modifiers ...func(s *sql.Selector)) *CaptureLogSelect {
	cls.modifiers = append(cls.modifiers, modifiers...)
	return cls
}
